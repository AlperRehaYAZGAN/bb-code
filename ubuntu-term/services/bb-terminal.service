[Unit]
Description=bulutbilisimciler-terminal-v1.0.0
After=network.target

[Service]
ExecStart=/bin/bash -c 'cd /root/workspace && while true; do /usr/bin/bb-terminal -p 40020 -m 64 --writable -t disableLeaveAlert=true bash; done'
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target